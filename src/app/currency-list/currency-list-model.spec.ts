import { CurrencyListModel } from './currency-list-model';

describe('CurrencyListModel', () => {
  it('should create an instance', () => {
    expect(new CurrencyListModel()).toBeTruthy();
  });
});
