import { CurrencyListEffect } from './currency-list-effect';

describe('CurrencyListEffect', () => {
  it('should create an instance', () => {
    expect(new CurrencyListEffect()).toBeTruthy();
  });
});
