import { CurrencyListSelector } from './currency-list-selector';

describe('CurrencyListSelector', () => {
  it('should create an instance', () => {
    expect(new CurrencyListSelector()).toBeTruthy();
  });
});
