import { CurrencyListReducer } from './currency-list-reducer';

describe('CurrencyListReducer', () => {
  it('should create an instance', () => {
    expect(new CurrencyListReducer()).toBeTruthy();
  });
});
