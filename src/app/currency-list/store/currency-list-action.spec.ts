import { CurrencyListAction } from './currency-list-action';

describe('CurrencyListAction', () => {
  it('should create an instance', () => {
    expect(new CurrencyListAction()).toBeTruthy();
  });
});
